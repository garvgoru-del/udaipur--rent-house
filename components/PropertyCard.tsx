import Image from 'next/image';import Link from 'next/link';import { Heart, MapPin, ShieldCheck, Star } from 'lucide-react';import type { Property } from '@/lib/data';
export function PropertyCard({p}:{p:Property}){return <article className="overflow-hidden rounded-3xl border border-blue-100 bg-white shadow-sm transition hover:-translate-y-1 hover:shadow-glow"><div className="relative h-56"><Image src={p.image} alt={p.title} fill className="object-cover"/><button aria-label="Add favourite" className="absolute right-3 top-3 rounded-full bg-white/90 p-2 text-royal"><Heart size={18}/></button>{p.premium&&<span className="gold-gradient absolute left-3 top-3 rounded-full px-3 py-1 text-xs font-black text-navy">Premium</span>}</div><div className="space-y-4 p-5"><div><div className="flex items-center justify-between"><p className="text-2xl font-black text-navy">₹{p.price.toLocaleString('en-IN')}<span className="text-sm font-medium text-slate-500">/mo</span></p><span className="flex items-center gap-1 text-sm font-bold text-gold"><Star size={16} fill="currentColor"/>{p.rating}</span></div><h3 className="mt-2 text-lg font-extrabold text-navy">{p.title}</h3><p className="mt-1 flex items-center gap-1 text-sm text-slate-600"><MapPin size={15}/>{p.locality}</p></div><div className="flex flex-wrap gap-2 text-xs font-bold text-slate-700"><span>{p.beds || '—'} beds</span><span>{p.baths} baths</span><span>{p.area} sq.ft.</span><span>{p.type}</span></div><div className="flex items-center justify-between"><span className={`flex items-center gap-1 text-xs font-bold ${p.verified?'text-emerald-600':'text-amber-600'}`}><ShieldCheck size={15}/>{p.verified?'Verified':'Pending verification'}</span><Link href={`/properties/${p.id}`} className="rounded-full bg-royal px-4 py-2 text-sm font-bold text-white">View details</Link></div></div></article>}
